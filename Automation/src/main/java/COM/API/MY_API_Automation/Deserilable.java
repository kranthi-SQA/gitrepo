package COM.API.MY_API_Automation;

import org.testng.Assert;
import org.testng.annotations.Test;

import io.restassured.response.ResponseBody;

public class Deserilable {

	public String City;
	public String Temperature;
	public String Humidity;
	public String WeatherDescription;
	public String WindSpeed;
	public String WindDirectionDegree;
/*	Apitest_weather api;

	@ Test
	public void validataresponse()
	{
		api=new Apitest_weather();
		ResponseBody responsebody = api.getweatherdetails();
		Apitest_weather values = responsebody.as(Apitest_weather.class);

		Assert.assertEquals("Hyderabad",City);
		Assert.assertEquals("Hyderabad",Temperature);
		Assert.assertEquals("Hyderabad",Humidity);
		Assert.assertEquals("Hyderabad",WeatherDescription);
		Assert.assertEquals("Hyderabad",WindSpeed);
		Assert.assertEquals("Hyderabad",WindDirectionDegree);



	}
*/





}
